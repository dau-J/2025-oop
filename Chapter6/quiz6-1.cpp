#include <iostream>
#include <cmath>
#include <cctype>

using namespace std;
int main(){
/*
    Quiz 6번
    */
   /*
   srand(time(0)) //이거 한번만 해줬더니 사람은 어떤수가 나올지 못알아맞춘다. 랜덤으로서의 자격은 유지지

   for(int i=0; i<10; i++){
   cout << rand()%10 << endl; //단점은 이미 랜덤수 발생순서가 정해져있다는거. 
   만약 0-9 사이의 수만 나오길 바란다면 ==> rand()%10
   만약 1-11 ==> rand()%10 +1
   만약 1-6 ==> rand()%6 + 1
   5 - 15 ==> rand()%11 + 5

   % 바로다음 나오는 숫자는 범위 안에 있는 숫자의 count ==> high - low + 1
   그리고 전체 이동하는 수는 low

   결론 ==> rand()% (high-low+1)+low
   }
   */
}